import React from 'react';

const WorkHistory = (props) => {
    const { WorkHistory } = props

    return (
        <li>
            Work history Input goes here
        </li>
    );
}


export default WorkHistory;